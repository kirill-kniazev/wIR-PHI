# get repo directory for future reference
import os, sys
from pathlib import Path
repo_dir = Path(__file__).resolve().parent

print (str(Path(__file__).resolve().parent / "c" / "get_images_parallel_high_speed_dynamic.dll"))